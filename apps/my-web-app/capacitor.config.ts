import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MyWebApp',
  webDir: '../../dist/apps/my-web-app',
  bundledWebRuntime: false,
};

export default config;
